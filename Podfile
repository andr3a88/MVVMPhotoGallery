platform :ios, '9.0'

target 'Photo Gallery' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'SDWebImage', '~> 4.0'
  pod 'SwiftLint'

  target 'Photo GalleryTests' do
    inherit! :search_paths
  end

  target 'Photo GalleryUITests' do
    inherit! :search_paths
  end

end
